# Used by the /api/summarize serverless function. Set these in the Vercel
# dashboard (Project → Settings → Environment Variables) for production, and
# copy this file to .env.local for `vercel dev` locally.
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_MODEL=deepseek-chat
